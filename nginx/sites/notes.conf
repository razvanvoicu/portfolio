server {
    listen 8080;
    server_name notes.raz notes.raz.sg;

    root /srv/www/notes;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    include /etc/nginx/stats-location.conf;
}
