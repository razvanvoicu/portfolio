server {
    listen 8080;
    server_name qr.raz qr.raz.sg;

    root /srv/www/qr;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    include /etc/nginx/stats-location.conf;
}
